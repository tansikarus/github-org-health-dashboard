{
  "name": "github-org-health-dashboard",
  "version": "1.0.0",
  "description": "A community manager's dashboard for tracking GitHub org repo health, stale repos, open issues, and AI-generated action summaries.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "gh-pages": "^6.1.1",
    "vite": "^5.1.0"
  }
}
